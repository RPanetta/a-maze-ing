from setuptools import find_packages, setup

setup(
    name='mazegen',
    version='1.0.0',
    description='Maze generation and solving library for the a-maze-ing '
    'project.',
    packages=find_packages(),
    python_requires='>=3.8',
)
